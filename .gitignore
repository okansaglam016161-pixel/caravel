# Secrets - never commit
.env
.env.*
*.token
*.key
secrets/

# Dependencies & build output
node_modules/
dist/
build/
.vite/

# ...but the vendored built dists are committed on purpose (see vendor/README.md) — re-include them
!vendor/**/dist/
!vendor/**/dist/**

# Local tari.js reference (if linked here)
tarijs-reference/

# Private working notes — kept locally, not committed
PROJECT_STATE.md

# macOS
.DS_Store

# Logs & editor, local tooling
*.log
.vscode/
.idea/
.claude/
